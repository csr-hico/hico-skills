"""Example bundled script. Files like this ride along with the skill and are fetchable via the
`get_resource` tool or the `file:///skills/<id>/scripts/example.py` MCP resource."""


def main() -> None:
    print("hello from a bundled skill script")


if __name__ == "__main__":
    main()
