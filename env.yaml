# Non-secret, non-revealing defaults only. Synced to Coolify env at deploy time.
# Everything that names a host/domain/issuer/group/client_id lives in secrets.enc.yaml.
PORT: 8000
LOG_LEVEL: INFO
BRAND_ORANGE: "#FF5F2C"
BRAND_BLUE: "#2C53AB"
BG: "#FFFFFF"
